RewriteEngine On

# Don't touch WordPress CMS paths — let WP serve them from /cms
RewriteRule ^cms(/|$) - [L]

# Static frontend files live at the document root.
DirectoryIndex index.html
ErrorDocument 404 /404.html
